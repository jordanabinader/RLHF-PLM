# Requirements for the Personalization Module

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
torch>=2.0.0
scikit-learn>=1.0.0

# Optional but recommended
tqdm>=4.65.0  # Progress bars during training
matplotlib>=3.5.0  # For visualization
seaborn>=0.12.0  # For plotting

# For specific tasks (install as needed)
# transformers>=4.30.0  # For ESM models
# esm>=2.0.0  # For ESM embeddings
# fair-esm>=2.0.0  # Alternative ESM installation
# peft>=0.4.0  # For LoRA adapters

